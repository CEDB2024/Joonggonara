version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/react
    overwrite: yes
permissions:
  - object: /var/www/react
    owner: ubuntu
    group: ubuntu
    mode: 755
hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 60
      runas: root
